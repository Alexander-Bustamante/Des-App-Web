console.log("Vinculado correctamente...")

function Suma_indices () {
    let indices = [1,2,3,4,5,6,7]
    suma_i = indices[indices.length -1] + indices[indices.length -2] + indices[0]
    alert(suma_i)
}